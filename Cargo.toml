[package]
name = "sorseal"
version = "0.1.0"
edition = "2021"
description = "Provenance for Soroban/WASM artifacts — prove deployed bytecode matches source."
license = "MIT"
authors = ["BreachDirect"]
rust-version = "1.85"
repository = "https://github.com/BreachDirect/sorseal"
readme = "README.md"
keywords = ["soroban", "stellar", "wasm", "provenance", "reproducible-builds"]
categories = ["development-tools::build-utils", "command-line-utilities"]

[lib]
name = "sorseal"
path = "src/lib.rs"

[[bin]]
name = "sorseal"
path = "src/main.rs"

[dependencies]
anyhow = "1.0.104"
base64 = "0.23"
clap = { version = "4.6", features = ["derive"] }
ed25519-dalek = { version = "3.0", features = ["rand_core"] }
getrandom = { version = "0.4", features = ["sys_rng"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.11"
stellar-strkey = "0.0.18"
toml = "1.1"
ureq = { version = "3.3", features = ["json"] }

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = "thin"
strip = true
