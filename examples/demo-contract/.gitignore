# Soroban demo contract (examples/demo-contract)
# Note: Cargo.lock and sorseal.provenance.json are intentionally committed —
# a sealed build must be reproducible (locked dependency graph), and the
# provenance file is a checked-in reference fixture restored by scripts/demo.sh.
target/
sorseal.key
sorseal.pub
sorseal.sarif